# 更新日志

本项目的显著变更记录在此文件。版本号遵循 `X.Y.Z`（合宙 FOTA 兼容的三段式）。

## 1.2.0 - 2026-09-12

### 新增

- 心跳报平安：`信鸽，设置心跳，<小时>`（1-168，支持中文数字）定时向
  全部已启用转发目标推送在线摘要（信号+转发统计）；默认关；新增
  `sp_heartbeat` 模块，与转发共用队列/限速/通道
- 运行统计：`sp_forward` 四路计数（累计转发/失败/丢弃/过滤），
  `信鸽，统计` 查看、`信鸽，清零统计` 归零（重启自动清零）
- 转发过滤：`信鸽，拉黑，<号码>`（上限10）与 `信鸽，添加过滤词，<词>`
  （可批量，1-10 字/词），命中静默丢弃并计数；只作用于转发路径，
  不影响命令鉴权
- 验证码提取：正文含触发词时转发首行附 `[验证码:xxxx]`（首个独立
  4-8 位数字串，跳过手机号/长单号），`信鸽，开启/关闭验证码` 开关
  （默认开）
- 失败暂存重发：全部通道失败的转发暂存 fskv（≤5 条，仅失败时写入
  护闪存寿命），网络恢复（IP_READY）自动重发或 `信鸽，重发` 手动
  排空；重试一次仍失败永久丢弃；随恢复出厂清除
- 断网自愈：`sp_net` 每 5 分钟轮询注册状态，连续 30 分钟未注册自动
  重启（无触发次数上限）；`信鸽，状态` 新增 心跳/过滤/验证码提取 三行

### 修复

- `tonumber(sp_at.digits(...))` 多返回值陷阱：digits 的 gsub 替换数
  被当进制参数导致心跳间隔恒解析失败（先落局部变量截断）
- `cmd_ch_on/off/clr` 对 `sp_channels.get(key)` 补 nil 守卫；
  `sp_channels.format_text` 验证码行调用名修复

## 1.1.0 - 2026-09-11

### 新增

- 来电提醒：收到来电时向全部已启用的转发目标推送提醒（含来电号码，
  固件不接听不挂断，纯 CC_IND 事件监听）；同一通电话多次响铃只提醒
  一次（会话去重 + 120s 兜底复位）；`信鸽，来电提醒` 查询、
  `信鸽，开启/关闭来电提醒` 开关（默认开，随初始化生效）。与短信转发
  共用队列、限速与防环标记。多数物联网卡未开通语音则不触发，无害
- README FAQ 补充避免漏接建议：手机侧设置未接通转移 `**61*<手机号>#`
  两全其美（模块照常提醒、无人接听转手机）；无条件转移 `**21*` 会让
  模块收不到来电事件、提醒不触发

## 1.0.0 - 2026-09-11

首个正式版本。

### 新增

- 全本地运行的 Air780EHV 短信转发固件：配置与控制仅通过短信完成，
  不依赖任何远程管理 API
- AT 命令风格的短信命令：`AT+INIT` `AT+ST?` `AT+WL` `AT+PW`
  `AT+FWD` `AT+RESET` `AT+REBOOT` 等（见 docs/commands.md）
- 三重独立门禁：IMEI 初始化、白名单（默认开启）、密码前缀模式；
  多门禁同时开启时须全部满足
- 转发通道：短信（离线可用）、钉钉 Webhook（加签）、飞书 Webhook（签名）、
  Server酱，插件式注册表可扩展
- 防捡漏自毁：SIM 卡变更、连续第 3 次无卡开机自动恢复出厂
- 纯 Lua 5.3 单元测试套件（含 LuatOS API 桩与 main.lua 装配冒烟），
  GitHub Actions CI（lua5.3 测试 + luacheck 静态检查）
- 状态灯支持（整机开发板 NET 灯，GPIO27）：网络/初始化状态指示与
  短信到达三连闪；修复应答短信错过开机 SMS_READY/CC_IND 广播后
  每条白等 30 秒的问题
- **破坏性变更**：短信命令全面改为中文句子格式，唤醒前缀 `信鸽`
  （如 `信鸽，状态`、`信鸽，初始化，<IMEI>`）。实测运营商/物联网卡平台
  过滤机器特征短信（`鸽+FWD=SMS,ADD,号码` 被拦），而全中文句子可达；
  此前的 `AT+`/`鸽+` 前缀格式完全移除，旧命令按普通短信转发；
  已初始化设备升级后配置保留，改发中文命令即可
- 修复并发发送竞态：短信 modem 同一时刻只允许一个在途发送，命令应答
  与远程发短信并发时后者撞内核 "sms is busy" 而丢失（真机实测丢过
  命令应答）；sp_platform 发送路径加互斥串行化，所有出栈短信排队
- 文档：说明呼叫转移无法由固件设置（LuatOS 无 USSD/CCFC 接口），
  给出手机一次性设置 **21* 随 SIM 生效的替代方案
- 新增远程发短信命令：信鸽，发送短信，<号码>，<内容>（号码支持中文
  数字与短号码，内容逗号按原样保留；受三重门禁保护）
- 短信发送路径重构：统一收敛到 sp_platform（就绪等待/SMS_SENT 日志），
  sp_chan_sms 转发补上就绪等待；修复 sp_chan_sms 中 failed 变量未声明
  导致失败路径运行时错误被 dispatch pcall 吞掉的缺陷
- dispatch 正确区分"通道返回失败"与"执行异常"，失败不再误记为成功；
- 安全与健壮性评审修复（详见提交历史 64b7e27..4ffc0c6）：
  - IMEI 不可读时拒绝初始化（堵空串比对接管）；空白名单拒绝开启
    （防锁死）；清空通道保留默认形状（修复清空后加号码异常）
  - 日志脱敏：收短信只记号码+长度；sms.debug 默认关闭，新增
    信鸽，调试，开/关 运行时开关；README 增加贴日志前脱敏提示
  - 转发两层防环：跳过本机 MSISDN 目标 + 随机实例标记（回环丢弃）；
    转发改单 worker 有界队列（上限 20、丢最旧计数、2s 限速）
  - SIM 热插拔回调改 timerStart（回调内 sys.wait 违规）；fskv 写
    失败记警告；Server酱 key ≥16 位；设置密码应答不回显明文；
    全角小写字母与全角空格归一；dispatch 契约统一 true 才成功
  dispatch 返回各通道结果表 results[key]（测试可断言通道成功，
  堵住"发送后抛错被 pcall 吞掉仍全绿"的测试缺口）
- 新增设备标识（identity）：多设备转发同群时区分来源；默认自动取 SIM
  本机号码尾 4 位（物联网卡未写 MSISDN 时不携带），可自定义/关闭/恢复自动
  （信鸽，标识/设置标识/关闭标识/清除标识），渲染为首行 [设备:xxx]
- 前缀与密码防冲突：转发前缀不可与命令前缀（信鸽/鸽/AT/密码）同头，
  设置密码时同样校验（防转发内容被解析为命令）
- 转发消息默认不携带【SmsPigeon】固定前缀（出栈短信的固定标识易被
  运营商过滤/拉黑）；新增可自定义转发前缀命令：信鸽，设置前缀/清除前缀/
  前缀（默认空，Server酱标题同步使用）
- 新增企业微信通道（消息推送，原"群机器人"）：`信鸽，设置企业微信，<key>`
  （纯 key 或完整 webhook，接口文档 path/99110）
- 钉钉/飞书支持纯 token 配置（`信鸽，设置钉钉，<access_token>`、
  `信鸽，设置飞书，<hook id>`）：webhook 完整 URL 的
  http://、access_token= 特征可能被运营商内容过滤拦截，纯 token
  只含字母数字横线，固件自动拼出标准 webhook 地址
- 号码/IMEI 支持中文数字（一三二六二五七五七一八，含幺/两/〇）与
  分组写法（132-6257-5718）；密码支持中文（按字符数计长，4-16 字符），
  密码不可为 `信鸽`
- 修复 Lua UTF-8 字节类地雷：多字节字符放入 `[]` 字符类会退化为字节
  集合（"态"的 80/81 字节曾被 `[、。]` 吃掉），改为完整字面量/逐字符
  模式匹配；全角归一、顿号句号归一、句尾 ？；！ 不算命令内容；
  短信中心时间戳日志修复 3GPP 两位年份（26→2026）并附带时区
- 短信可观测性增强：开启官方内核短信调试日志（sms.debug）；每条收到的
  短信打印短信中心时间戳（SCTS）用于判定运营商延迟投递；应答与转发
  发送结果携带 error_code/rp_cause 诊断码